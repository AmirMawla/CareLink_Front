/*interface user {
  id: number;
  first_name : string;
  last_name : string;
  username : string;
  email : string;
  password : string;
  role : string;
  is_active : boolean
}

interface Doctor {
  user : user;
  specialty : string;
  session_duration : number;
  buffer_time : number;
}

interface Patient {
  user : user;
  date_of_birth : Date;
  phone_number : string;
  medical_history : string
}

interface Receptionist{
  user : user;
  doctor_id : number;
}

interface Admin{
  user : user
}

export interface APIResponse<T> {
 
}*/